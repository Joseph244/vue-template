const dict = {
    state: {},
    mutations: {
        // SET_FILETEMPLATEPATH(state, val) {
        //     state.FILETEMPLATEPATH = val;
        // }
    },
    actions: {
        // async fileTemplatePath({ commit }) {
        //     const data = await toolsApi.fileTemplatePath();
        //     commit('SET_FILETEMPLATEPATH', data);
        // }
    }
};
export default dict;
