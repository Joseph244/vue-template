import defaultSettings from '@/settings';

const { menuPosition } = defaultSettings;

const state = {
    menuPosition
};

const mutations = {};

const actions = {};

export default {
    namespaced: true,
    state,
    mutations,
    actions
};
